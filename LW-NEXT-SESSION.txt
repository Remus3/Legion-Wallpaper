NEXT SESSION
------------
Task: /first-pass on the 17 slugs intaken 2026-09-14 (single AI upscale via
  IllustrationJaNai V3 in .venv-upscale, Lanczos down to >= 2560x1440, light
  USM, G1 gate, submit to _firstneedauth). Every one is 1024-1920 wide.
Context: images/1.First Pass Scratch (135 slugs; the 17 new ones carry a
  "recovery" block in their manifest ANNOTATE audit). Best source is NOT the
  _firstinitial - the quota-free refetch at
  data/recovery/fetched/<slug>/deviantart/<artist>/*.jpg has the same pixels
  with 4-7x fewer JPEG artifacts, and lw_first_pass.find_fetched_fullview
  already globs it. Probe live state first: python tools/lw_pipeline.py status
  and scan (anomalies 0, pending_intake 0 at wrap). The parked queue in
  docs/NEXT_SESSION_PARKED_2026-09-10.md is still open behind this (108
  needauth, 7 aspect_crop_heavy, 3 lap_ratio) - re-probe its counts, they have
  moved.
Acceptance: each of the 17 has a _firstworking_## plus a G1 gate verdict
  recorded in its manifest, PIPELINE_LOG carries SAVE_WORKING + ANNOTATE +
  SUBMIT per slug, python -m pytest tests/ -q green (2929 passed / 18 skipped
  at wrap), ruff clean, drift_guard exit 0, done_gate bind exit 0.
Do NOT redo: source recovery on these 17 - Tier 1 hit 17 of 17, is recorded in
  every manifest and the bytes are fetched. The gallery_dl_fetch zero-exit
  defect and the gitignored-provenance defect are both fixed and backfilled in
  bda44d5. The 6 files that vanished from 0.Originals were deleted by the
  operator by hand, not by a tool - do not re-investigate. DeviantArt preview
  watermarks on this batch are NOT yet assessed; the 2026-09-01 measurement
  dropped 8 of 23 as unsalvageable, so expect cleaning-stage drops later and
  do not read them as a cleaning bug.
Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES +
  git log.
