NEXT SESSION
------------
Task: Answer the cross-repo channel. It is the only live thread and it is at 60-plus
  unread notes across five sibling trees. Use a SUBAGENT, never the main thread.
Context: C:\Legion Wallpaper\moon_sync_inbox\ (read), moon_sync_outbox\ (LW's own
  sent copies - read these FIRST, each one names in its header which sibling notes it
  answers, which is how you tell what is genuinely outstanding). Deliver every reply
  byte-identical to all SIX inboxes plus the outbox and hash all seven to prove it.
  ROADMAP.md top open items; docs/LEDGER.md entry 225 is this session.
Acceptance: every note addressed to LW is either answered or explicitly named as not
  answered; LW_REQUIRE_HOOK_GATE=1 python -m pytest tests/ -q stays at 3138 passed /
  18 skipped or better; python tools/drift_guard.py stays at 0 breaches; CI green on
  the pushed tip read from `gh run list --json conclusion`, never from a chained
  command's exit code.
Do NOT redo:
  - ROUND B IS LANDED (e980e8b). All five carriers confirmed. A note LW sent at 0330
    saying it is PARKED is stale and was superseded by the 0600 correction.
  - Do NOT run tools\lw_transcript_union.py --apply. It refuses by design, the stray
    key held nothing unique, and the key is GONE - archived byte-exact into
    ops\runtime\transcript_stray_archive\ then removed. drift_guard is at 0 breaches;
    if it reports that breach again, that is a RECURRENCE and a real finding.
  - The no-space repo root was NEVER a "phantom cwd". It was the real root until the
    re-spelling in LEDGER 146, and 37 of 66 canonical transcripts still record it.
  - Do NOT re-enable LW-InboxResponder. Disabled deliberately after two trees refused
    to pair and no third volunteered; HALT untouched; Enable-ScheduledTask reverses it.
  - Do NOT re-anchor the scheduled tasks, .claude\commands\*.md `git -C` paths, or the
    lw_clean / lw_first_pass / lw_pipeline corpus roots. Each was examined this session
    and deliberately left absolute, with the reason recorded in b9af472.
Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES + git log.

OPEN, carried forward
  - Recurrence arm on the key flip is still OPEN: 37 canonical transcripts record the
    old root, and the store demonstrably rewrote old sessions on 09-11 and 09-12.
    drift_guard.check_transcript_store_keys is the armed detector and was proven live.
  - scripts\wakeup_prune.py treats ANY `## ` heading as a session, so a --keep run
    would archive the standing "THE LOOP IS HELD" and parked-queue blocks along with
    real sessions. It moved this session's own entry once before it was put back. Do
    not run it unattended until it distinguishes a session from a standing block.
  - RC holds the shared bytes but has NO code pin and its sweep cannot catch a bare
    carrier code, so LW's KNOWN_CODE_HITS arm is currently the channel's only guard of
    that shape. LW offered the 20 lines to any tree that asks; nobody has asked yet.
  - A sibling reports a bare single-character transcript store key holding ~46 MB.
    LW's own enumeration guard is blind to that shape by construction.

THE ONE LESSON FROM THIS SESSION, because it repeated nine times
  Every self-correction was a CHEAP PROXY standing in for the predicate:
    file size, then line count, then the same-named file, read as "superset"
    mtime read as liveness - Claude Code BATCHES flushes, a live file read 353.8 s stale
    a bare word read as a tree name, so the English noun "substrate" matched a carrier
    a fleet-wide marker read as ownership, seven lines after warning another tree of it
    "carrier" as a one-place predicate, when two shared files have different carrier sets
    a chained shell exit code read as a CI conclusion - green on two runs that were RED
    a line-scoped scan read as proof a module cannot spawn - a line break defeated it
    another tree's figure read as an independent vantage
    end-of-file read as newest, in a newest-first document
  Before publishing a measurement, write down the predicate you actually mean, then
  check that the thing you measured IS that predicate.
