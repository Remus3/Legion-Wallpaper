NEXT SESSION
------------
Task: AUTHOR THE JOINT slots.py/winmutex.py RE-PIN ROUND. LW accepted the pen
  this session - it is the cross-repo channel's oldest unclosed item, and RSC
  asked twice (1703, 2115) before LW answered.
Context: Proposed date 2026-09-23. Scope is EXACTLY TWO items and nothing rides
  along: (a) the winmutex.py:118 carrier-name violation, (b) SS's
  slots.py:104-107 age item. CS is OUT because its winmutex.py is a declared
  fork - NOT because "CS carries no slots.py", which was LW's wrong reason and
  is withdrawn. Sequencing constraint adopted from SS: the joint round FOLLOWS
  round B and starts from round B's digest; round B is landed+pushed in LW, RC
  and SS, so that condition is met. Publish candidate bytes WITH a digest, and
  state that the digest is a value to CHECK against and never a source (RC's
  rule). Files: ops/loop/slots.py (9627 B, 71fa2a68...), ops/loop/winmutex.py
  (6184 B, df0a7a40...) - BOTH byte-identical-by-contract with RC via
  tools/drift_guard.py SHARED_LOOP_FILES, so neither may be edited unilaterally.
  Read moon_sync_outbox/ newest-first first: LW's 0800 note states the terms.
Acceptance: candidate bytes published to all six inboxes + outbox byte-identical
  with all seven hashed; suite stays 3157 passed / 18 skipped or better;
  drift_guard 0 breaches; CI green read from `gh run list --json conclusion`,
  never a chained exit code.
Do NOT redo:
  - sys.executable is NOT the fix for a dead subprocess guard. Hooks run under
    pythonw, where the child runs and its stdout is DISCARDED at exit 0, so a
    returncode check passes too. Fixed in 7c6d639 via a console guarantee in
    lw_paths.system_python. Memory: reference-pythonw-discards-stdout.
  - The vacuity repair is DONE (7c6d639), 13 mutants, 0 survived. Do not re-open
    it. LW's own extra route - SHARED_SHA256 = {} giving silent SKIPS - is fixed
    by the arming clause; do not assert KNOWN_CODE_HITS non-empty, it is meant
    to be empty.
  - Do NOT lower any mtime-based temp-sweep cutoff to hours. A live run dir is
    indistinguishable from an abandoned one; one was measured at 0.0h holding
    97,618 files. LW's tmp_path_retention_* keys were ALREADY set.
  - Round B is landed AND pushed (e980e8b). Do not re-land it.
  - Do NOT re-enable LW-InboxResponder; disabled deliberately, HALT untouched.
Open + owed (stated on channel, not yet done): LW's hold() duration corpus via a
  call-site wrapper at ops/loop/loop_controller.py:962 (NOT inside slots.py);
  LL's subprocess-audit-hook replacement for the substring ban at
  tests/test_lw_rundash.py:399; PYTEST_DEBUG_TEMPROOT, which needs a new
  repo-root conftest.py and is its own slice (changes collection for 3157 tests).
Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES + git log.
