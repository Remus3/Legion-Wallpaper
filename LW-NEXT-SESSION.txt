NEXT SESSION
------------
Both QA gates from the 2026-09-19 sweep are CLOSED and the main first-pass
task is DONE. Do not redo either - see LEDGER 214. What is left is below.

FIRST, the only thing blocked on a human:

  The 17 first-pass submissions await approve/reject.
  All 17 sit at <slug>_firstneedauth.png in images/1.First Pass Scratch.
  First pass never self-approves - that is the contract, not an oversight.
    python tools/lw_pipeline.py approve <slug>
    python tools/lw_pipeline.py reject  <slug> --note "<reason>"
  14 gated PASS. 3 gated FLAG, all of them band_delta a hair over the 0.05
  threshold, and all three submitted with their report as the gate allows:
    leona-by-stellastria-dm0rd6j-fullview    0.0574
    miss-fortune-by-stellastria-dkul8k8-fullview  0.0505
    sona-by-stellastria-dmiu77x-fullview     0.0504
  Every one of the 17 is exactly 2560x1440, MS-SSIM 0.998462-0.999642,
  LPIPS 0.00178-0.013788. If the operator is not available, do NOT approve
  on their behalf and do NOT re-run the pass to try for better numbers -
  a further pass measurably DEGRADES (clean-retry-degrades).

THEN the open LW row QA-2 uncovered, which is real work:

  LW carries a duplicate transcript key and its own checker cannot see it.
  `.claude/projects/` under the user profile holds BOTH spellings:
    C--Legion-Wallpaper    270 files  471.5 MB   <- the real one
    C--LegionWallpaper       4 files   12.8 MB   <- stray
  CS found this from OUTSIDE, in a size table, on 2026-09-19.
  drift_guard.check_claude_path_keys did not, because it grades whether the
  spellings it FINDS agree on a trust bit and never ENUMERATES spellings -
  agreement is trivially satisfied when only one spelling is examined.
  Two pieces of work, in this order:
    1. Close the checker gap TDD-first: a failing test that plants two
       spellings for one directory and asserts the checker REPORTS it.
       Prove the arm binds (kill a mutant, restore byte-exact) and prove
       the report CLEARS when the duplicate is removed - "appears" is not
       "goes away".
    2. THEN decide the stray key's fate. It is NOT a prune-on-sight:
       4 transcript files are the record of 4 real sessions, and LW spent
       this same week learning that small-old-and-unrecognised is not a
       liveness measure (the ll-captures withdrawal). Decide it on what
       references it, not on its size or its age.
  Worth broadcasting the checker-shape finding once fixed: any tree running
  a similar check has the same enumerate-vs-compare hole.

CARRY FORWARD, unchanged:
  - The headless loop is still HELD. Operator 2026-09-16: RC and CS only.
    A session that reads the earlier looping instruction is acting on a
    superseded order. WAKEUP_NOTES has the kept prompt, armed only by the
    operator saying so.
  - LW's ll-captures PRUNE recommendation was WITHDRAWN. Do not re-assert.
  - C:\LW-backups\lw-pre-email-scrub-2026-09-07.bundle is REFERENCED-AND-IDLE
    (the pre-rewrite object graph from the email purge). Do not prune it.
  - The cathedral-syndra crop is a DECIDED question: top=60, recorded in the
    manifest as crop_sides {"top": 60} and in
    ops/runtime/crop_overrides_cathedral.json. Do not re-derive it.
  - Source recovery on the 17 is done (Tier 1 hit 17/17) and lw_first_pass
    already prefers the fetched DeviantArt fullview over _firstinitial - the
    hand-off advice that named this is now implemented, not pending.

STATE AT WRAP (re-measure anything you act on):
  pytest tests/ -q     3025 passed, 18 skipped
  ruff check .         clean
  drift_guard          0 breaches, 6 notes
  git                  clean on main; images/** and PIPELINE_LOG.md are
                       gitignored, so the image work has no commit by design
  inbox                0 unread; 2 LW replies filed to all five trees

Start with: /clear, then CLAUDE.md + MEMORY.md + WAKEUP_NOTES + git log
  + docs/LEDGER.md entry 214 + this file.
