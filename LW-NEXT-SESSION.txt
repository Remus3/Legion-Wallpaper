NEXT SESSION
------------
Task: ROADMAP high-priority `verbatim-payload-followups` row (1) is still
  BLOCKED - do NOT start it. Pick the next open ROADMAP item instead; the
  gate-ordering row is DONE and the payload row (2) is CANCELLED, so the top of
  ROADMAP.md is the place to choose from.
Context: ROADMAP.md "Open items - High priority", BACKLOG.md for aspirational
  work. Inbox first: moon_sync_inbox/ prints unread at SessionStart; read THEN
  ack with `python tools/lw_facts.py --mark-inbox-seen`.
Acceptance: whatever the chosen item states. Standing bar: `python -m pytest
  tests/ -q` green (baseline 2693 passed / 18 skipped, ~146s on 2026-09-07),
  ruff clean, drift_guard exit 0.
Do NOT redo: the /done gate ordering is FIXED and pushed (LEDGER 171).
  `.claude/commands/done.md` is renumbered - the binding gate is section 7, the
  hand-off write is section 6 (was 10b), section 0 is a NON-BINDING pre-flight.
  `tools/done_gate.py bind` + `verify-push` are the assertion; 14 arms in
  tests/test_done_gate.py, mutation-proven 6 of 6. Every future /done MUST run
  bind AFTER all doc edits are committed and verify-push after the push - the
  ritual doc's own section order is pinned by a test, so do not re-shuffle it.
  The `reap` arm for a stale `reserved-<key>.lock` stays BLOCKED until the five
  repos agree the short keys `rc lw rsc cs ll`.
Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES +
  git log.
