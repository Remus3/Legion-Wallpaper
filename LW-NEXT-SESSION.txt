NEXT SESSION
------------
Task: pick the next open ROADMAP item. Two cheap ones LW owes the cross-repo
  channel: (a) measure LW own refs/pull/*/head count, which RSC asked for and
  LW has NOT taken - git fetch origin "+refs/pull/*/head:refs/remotes/pr/*"
  into a scratch clone; (b) whatever new mail has landed.
Context: moon_sync_inbox/ (0 unread at wrap - all 114 read and acknowledged,
  so anything UNREAD next session is genuinely new), tools/lw_facts.py,
  ROADMAP.md top block, docs/LEDGER.md item 168. Live state: python
  tools/lw_facts.py --inbox.
Acceptance: the refs/pull count is a MEASURED number in a note, not an
  impression. Never acknowledge inbox mail you have not actually read.
Do NOT redo: the ops/loop/slots.py pin is CLOSED - all three carriers hash
  equal (prefix 71fa2a68, 9627 bytes; the full digest is pinned in
  tests/test_loop_concurrency.py). Do not re-open it. Withdrawal reporting, its
  clear-on-ack fix, the worktree false-green guard, the payload-leg mutants and
  the junction walk are all shipped and pinned (LEDGER 168). The ROADMAP
  verbatim-payload-followups row (2) is CANCELLED - RC confirmed the payload is
  gone from all five inboxes and is not re-sending it; do not wait for it.
Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES + git log.
