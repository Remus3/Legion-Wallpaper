NEXT SESSION
------------
FIRST, two QA gates from the 2026-09-19 stray-work sweep (LEDGER 213). Both
  are quick, both need a judgement call, and both should clear before the
  main task below starts.

QA-1: is it okay to DELETE the repo-root Claude/ directory?
  What it is: an Electron user-data profile (Cache, Code Cache, GPUCache,
    DawnWebGPUCache, Crashpad, IndexedDB, Local Storage, Network, Partitions,
    Preferences, Local State, and a Claude Extensions tree of 44,761 files).
    47,109 files / 1.2 GB. Newest mtime 2026-08-01. The desktop app was once
    launched with cwd set to the repo root and wrote its whole profile here.
  Why it is invisible: .gitignore:12 `/Claude/` hides it, so no git status
    has printed it since, and no clone carries it.
  The sweep classified it PRUNE but did NOT execute - that pass was read-only
    by operator instruction, so the deletion is still owed.
  QA before deleting, in this order:
    1. Re-measure it live (count, bytes, newest mtime). Do not trust the
       numbers above - they are dated 2026-09-19.
    2. Confirm the newest mtime is still 2026-08-01 and NOT today. A live
       writer changes the answer completely: a running desktop app pointed at
       this profile must not have it deleted underneath it. This is the same
       do-not-touch reasoning two siblings applied to ClaudeBackup_20260906.
    3. Confirm no LW code, config, task XML or .claude/settings.json
       references the path. `git grep -i "Claude/"` plus a check of
       ops/runtime/*.xml.
    4. Confirm the app is not running against it (tasklist for the desktop
       app; if it is, defer - do not kill it).
    5. Only then delete, and record the freed bytes in the LEDGER entry.
  If ANY of 2-4 fails: do not delete, say why, and leave it for the operator.
  Note this is LW's own bytes in LW's own tree - no sibling adjudication is
    needed, unlike the ~51 GB of sibling staging on the drive root, which is
    owner-to-confirm and NOT LW's to touch.

QA-2: process the moon_sync_inbox backlog.
  At 2026-09-19 wrap: 7 unread of 246, deliberately NOT acknowledged. The
    unread set includes RSC's sweep (arrived 1451, never opened), CS's sweep
    (only grepped for the rows that overlapped LW's), LW's own two outbound
    notes, and a CS FYI from 2026-09-16 that no session has read at all.
  Why it was left: `python tools/lw_facts.py --mark-inbox-seen` acknowledges
    what the REPORT showed, so marking seen with the CS FYI unread would clear
    a note nobody read. That is the exact defect ef3465a fixed - do not repeat
    it by acknowledging in bulk to get a quiet session-start banner.
  Do: read each unread note, answer anything addressed to LW, file replies
    into the sibling inboxes per docs/CHANNEL.md, and only then mark seen.
  Carry into any reply: LW's sweep recommendation on ll-captures was WITHDRAWN
    (an mtime is not a liveness measure - see memory feedback-mtime-is-not-
    liveness). Do not re-assert it, and do not propose PRUNE on any sibling's
    bytes from size and age alone.
  Open asks LW made and has not had answered by all five: UNKNOWN-row claims
    (Monster, ReplayBook, Ghost Toolbox, xnx, ClaudeBackup_20260906, _lt.txt,
    and C:\Tools\dewatermark + vtracer), each tree's own contribution to the
    git-install-root bucket, and each tree's key count in the harness
    per-project transcript store (the .claude/projects directory under the
    user config root - LW has TWO keys there for one tree).

THEN the main task, unchanged and still open:
Task: /first-pass on the 17 slugs intaken 2026-09-14 (single AI upscale via
  IllustrationJaNai V3 in .venv-upscale, Lanczos down to >= 2560x1440, light
  USM, G1 gate, submit to _firstneedauth). Every one is 1024-1920 wide.
Context: images/1.First Pass Scratch (135 slugs; the 17 new ones carry a
  "recovery" block in their manifest ANNOTATE audit). Best source is NOT the
  _firstinitial - the quota-free refetch at
  data/recovery/fetched/<slug>/deviantart/<artist>/*.jpg has the same pixels
  with 4-7x fewer JPEG artifacts, and lw_first_pass.find_fetched_fullview
  already globs it. Probe live state first: python tools/lw_pipeline.py status
  and scan (anomalies 0, pending_intake 0 at the 2026-09-14 wrap; re-probe,
  five sessions have run since). The parked queue in
  docs/NEXT_SESSION_PARKED_2026-09-10.md is still open behind this (108
  needauth, 7 aspect_crop_heavy, 3 lap_ratio) - re-probe its counts, they have
  moved.
Acceptance: each of the 17 has a _firstworking_## plus a G1 gate verdict
  recorded in its manifest, PIPELINE_LOG carries SAVE_WORKING + ANNOTATE +
  SUBMIT per slug, python -m pytest tests/ -q green (3025 passed / 18 skipped
  at the 2026-09-19 wrap), ruff clean, drift_guard exit 0, done_gate bind
  exit 0.
Do NOT redo: source recovery on these 17 - Tier 1 hit 17 of 17, is recorded in
  every manifest and the bytes are fetched. The gallery_dl_fetch zero-exit
  defect and the gitignored-provenance defect are both fixed and backfilled in
  bda44d5. The 6 files that vanished from 0.Originals were deleted by the
  operator by hand, not by a tool - do not re-investigate. DeviantArt preview
  watermarks on this batch are NOT yet assessed; the 2026-09-01 measurement
  dropped 8 of 23 as unsalvageable, so expect cleaning-stage drops later and
  do not read them as a cleaning bug.
Do NOT redo from the sweep: the machine-wide stray-work inventory itself. It
  is filed (LEDGER 213) and its numbers are dated - re-measure any row you act
  on, but do not re-run the whole pass. The two defects it found in LW
  (strip_em_dashes post-filter, unignored /worktrees/) are fixed in c9a02de.
  The headless loop is still HELD - see WAKEUP_NOTES, do not start it.
Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES +
  git log + this file.
