NEXT SESSION
------------
Task: Decide the Clockspeed call, then pick the next ROADMAP item. CS writes 23
  *.copy.sav files into its LIVE work/saves/ (tests/save/test_phase4_gate.py and
  tests/surface/test_acceptance.py copy the operator's real saves beside the
  originals instead of into tmp). Nobody is on it. The operator was asked at
  wrap whether LW should fix CS directly and had not answered - ask again before
  touching another repo's tree. LW has edited nothing in any sibling tree.
Context: tools/lw_write_tracer.py is the instrument - process-local, patches
  builtins.open + io.open + os.replace + os.rename, counts bytes, plants a
  control every run. Run it as:
  pytest tests/ -p lw_write_tracer --trace-roots "<dirs>" --trace-out out.json
  with the tool directory on PYTHONPATH. CS roots are ops;data;work;config;
  memory;moon_sync_inbox - CS has NO ops/runtime and NO logs, so watching those
  is a vacuous probe. Read control.proved in the report before believing a zero.
  Parked pipeline queue still sits in docs/NEXT_SESSION_PARKED_2026-09-10.md.
Acceptance: a traced CS run reports control.proved true and every finding bucket
  empty, or the operator has explicitly declined the CS fix.
Do NOT redo: LW is fixed and verified - 3 sites closed (ec9c8d6, f319583), suite
  traces clean at 2912 passed with control proved. Do not re-measure LW. Do not
  use a snapshot/mtime diff to attribute writes on this box: a 300s idle control
  proved it attributes nothing. RSC self-fixed at 7786955 and measures clean;
  RC has its own tracer and is on it; LL was told about its inbox ack state.
  The sync inbox lane itself is healthy - report, ack, fan-out and the armed
  responder were all probed live this session.
Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES + git log.
