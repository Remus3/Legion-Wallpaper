NEXT SESSION
------------
Task: ROADMAP first-pass-batch-2026-09-08 leftovers. All three queues are
  OPERATOR decisions - do not self-resolve any of them.
  (a) 108 slugs await approve/reject in _firstneedauth (never self-approved).
  (b) 7 slugs HELD aspect_crop_heavy need a per-image framing call (3 pintrest
      lose 68-71% of area to reach 16:9). Mechanism exists: --crop-overrides
      takes {slug: {"top": N}} for an exact offset (d327642).
  (c) 3 slugs failed G1 lap_ratio 0.91-0.93 vs floor 1.0 - soft SOURCES, not
      the double-resample bug; they kept their _firstworking.
  Ask the operator which of the three to work, then work only that one.
Also open, one command, operator's call (LEDGER 176): GitHub private
  vulnerability reporting is DISABLED and SECURITY.md plus the issue-template
  contact link both route to it. Enable with
  `gh api -X PUT repos/Remus3/Legion-Wallpaper/private-vulnerability-reporting`.
  Not run unattended: it is a repo settings change.
Context: ROADMAP.md "Open items - High priority" top row; PIPELINE_LOG.md;
  ops/runtime/{tier2_queue,intake_refused,crop_overrides}_2026-09-07.*;
  docs/LEDGER.md 173-176. Inbox first: moon_sync_inbox prints unread at
  SessionStart; read THEN ack with `python tools/lw_facts.py --mark-inbox-seen`.
Acceptance: whatever the chosen item states. Standing bar: `python -m pytest
  tests/ -q` green (baseline 2752 passed / 18 skipped, ~132s on 2026-09-08),
  ruff clean, drift_guard exit 0, `python tools/done_gate.py bind` exit 0.
Do NOT redo: the split-value guard work is SHIPPED (LEDGER 174, ae9b277) -
  tools/split_scan.py + tests/test_no_split_identity.py, values pinned by
  sha256, normalised + reversed views, per-value exemptions. The operator's
  address is out of the tree again; do NOT rewrite history for it. The /done
  gate now keeps a failing check's output in ops/runtime/done_gate_failure.log
  (LEDGER 175, c84b008) - do NOT pipe the gate through `tail`. The GitHub
  community checklist is COMPLETE (LEDGER 176, eacb64e) - five new files, do
  not re-author them. Tier 0 recovery was a REAL null for this batch.
Start with: /clear, then bootstrap from CLAUDE.md + MEMORY.md + WAKEUP_NOTES +
  git log.
